# End-to-End Validation

Track full-pipeline validation runs here. Include source names, DAG versions, and notable outcomes.

## Recent runs
- Placeholder entry: run details to be added when validation is executed.

Refer to `config/agents/pipelines.yaml` for configured sources and to the Airflow DAGs under `dags/` for execution entry points.
